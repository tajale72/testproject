
//we created user object based on input we get form HTML form field
export class User{
    constructor(
        name: string,
        email: string,
        experience: Number,
        domain:string,
        //Here all fields are from our HTML form so that
        //we can build a user object and pass it to the POST request
    ){
     
    }
}